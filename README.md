# Mlops activity

Scaffolded project for your MLOps activity.

Run the init script to set your Git config and make the initial commit:

PowerShell:

```
.\init_repo.ps1 -gitName "Your Name"
```

The script sets `user.email` to `khanahyan48@gmail.com` by default.
